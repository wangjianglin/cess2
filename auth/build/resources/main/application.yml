security:
  user:
    password: password
  oauth2:
    client:
      client-id: demo
      client-secret: '{noop}demo'
      scope: read, write
      auto-approve-scopes: .*
    authorization:
      check-token-access: permitAll()
server:
  port: 18002
#  context-path: /uaa
logging:
  level:
    org.springframework.security: DEBUG